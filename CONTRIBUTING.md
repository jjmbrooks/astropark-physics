# Contributing

1. Lee `AGENTS.md` y `docs/01` … `docs/08` (roles/Kanban en `08-ROLES.md`).
2. Respeta ADRs (`docs/05-DECISIONES.md`) y contratos de IDs/schema.
3. Un owner de código: **Codelius**. Slices mecánicos vía Kanban a **`runica`** cuando el perfil esté live.
4. No force-push a `main`. No secretos en el repo.
5. `npm run build` debe pasar antes de pedir review.

Handoffs Grok → NexIA: título `[Grok:Codelius] …`, assignee `nexia`.
