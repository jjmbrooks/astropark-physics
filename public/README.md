# public/

Assets estáticos servidos en la raíz del sitio (base `/astropark-physics/`).

- `assets/aliens/` — bustos WebP (+ derivatives `-256`)
- `assets/aliens/poses/` — poses idle/acción
- `assets/hero/` — banner del parque
- `assets/thumbs/` — miniaturas Lab/Arcade
- `assets/icons/` — sprite SVG + apple-touch
- `assets/og/` — imagen para compartir
- `audio/` — BGM (mp3/ogg) + SFX wav
- `favicon.svg`
